version: 2.1

#orbs:
#  win: circleci/windows@1.0.0

commands:
  test_command:
    parameters:
      gcc_version:
        type: integer
        default: 8
    steps:
      - run:
          name: "Test command"
          command: |
            echo "Test command"
            echo "Param: << parameters.gcc_version >>"
            sudo apt update
            sudo apt install -y g++-<< parameters.gcc_version >>
            echo "gcc version 1: $(gcc -v)"
            echo ""
            echo "gcc version 2: $(g++ -v)"
          shell: /bin/bash

jobs:
  test_1:
    machine:
      image: ubuntu-1604:201903-01
    steps:
      - run:
          name: "Test job"
          command: |
            echo "Call test_command"
          shell: /bin/bash
      - test_command:
          gcc_version: 9

workflows:
  version: 2
  test_workspace:
    jobs:
      - test_1
